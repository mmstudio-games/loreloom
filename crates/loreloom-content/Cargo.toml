[package]
name = "loreloom-content"
version = "0.1.0"
edition = "2024"

[dependencies]
loreloom-core = { version = "0.1.0", path = "../loreloom-core" }

[dev-dependencies]
semver = { version = "1", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
toml = "1"
