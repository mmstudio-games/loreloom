# Shared Test Data

This directory contains repository-wide input data for deterministic tests, including Mod Package,
save/recovery, Mock Agent, and TUI snapshot cases.

Test data is not production content and must never contain Provider secrets or live credentials.
