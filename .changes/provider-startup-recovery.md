---
loreloom: "patch:fix"
loreloom-tui: "patch:fix"
---

Keep interactive Provider setup failures in the terminal with retry, Settings, return to launcher, and quit. Preserve the pending save/player selection and reload configuration on retry before opening a Store.
