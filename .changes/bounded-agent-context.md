---
loreloom: "patch:feat"
loreloom-agent: "patch:feat"
loreloom-runtime: "patch:feat"
---

Apply configurable bounded projections to narrator and NPC contexts for transcript bytes and entries, visible actors, inventory, skills, knowledge, and goals. Mark truncated observations, reject oversized requests before provider I/O with a deterministic token estimate, filter forgotten facts, and validate host limits from strict product configuration.
