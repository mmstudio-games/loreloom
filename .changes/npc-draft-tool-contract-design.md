---
loreloom-agent: "patch:feat"
loreloom-content: "patch:fix"
loreloom-runtime: "patch:fix"
---

Define NPC generation as a single-purpose tool stage that ends on the first accepted draft, keeps
trusted agent bindings out of model input, accepts omitted empty collections, and reports only safe
field-level draft validation categories.
