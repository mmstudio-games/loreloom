---
loreloom: "patch:feat"
loreloom-agent: "patch:feat"
loreloom-runtime: "patch:feat"
---

Define an accepted `create_npc` call as a narrator turn barrier so character materialization starts
immediately without an unused model continuation, while preserving post-commit replanning.
