---
loreloom: "patch:feat"
loreloom-content: "patch:feat"
loreloom-core: "patch:feat"
loreloom-runtime: "patch:feat"
---

Project the root World, enabled ModLock entries, and safely inspected installed-but-disabled
directory packages into each UI snapshot. Reuse activation integrity checks, keep invalid or
unreadable installed candidates non-fatal and summarized, and exclude paths, hashes, and package
content from the catalog view.
