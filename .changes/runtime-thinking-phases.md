---
loreloom: "patch:feat"
loreloom-core: "patch:feat"
loreloom-runtime: "patch:feat"
loreloom-tui: "patch:feat"
---

Publish coarse runtime phase events throughout player-turn orchestration and render them as local animated thinking status. Remove Provider text streaming from the product UI protocol, preserve committed snapshots as the only source of story and world state, and keep submit, cancel, resize, and terminal-state handling responsive while model calls are in flight.
