---
loreloom: "patch:feat"
loreloom-agent: "patch:feat"
loreloom-runtime: "patch:feat"
---

Raise the default per-Agent and per-player-input Model, Tool, token, byte, turn, round, and elapsed
budgets so repeated tool continuations and multi-agent narration can complete without requiring
immediate configuration tuning.
