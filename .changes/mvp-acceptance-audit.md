---
loreloom: "patch:chore"
loreloom-runtime: "patch:fix"
---

Complete the first-stage acceptance audit, record the optimized product binary and durable headless
reopen baseline, and add regression coverage proving that provider failures are redacted while the
same Runtime remains usable for the next player turn. Keep deterministic physical save operations
explicitly gated on upstream shutdown support and public distribution gated on a compatible driver
license.
