---
loreloom: "patch:feat"
loreloom-content: "patch:feat"
loreloom-core: "patch:feat"
loreloom-runtime: "patch:feat"
loreloom-world: "patch:feat"
---

Add fixed, preset, and typed UGC player bootstraps. Content authors can declare deterministic
player forms whose validated fields compile into the existing character and parameter records,
including durable player-created provenance.
