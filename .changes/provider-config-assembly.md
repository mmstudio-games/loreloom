---
loreloom: "patch:feat"
---

Add strict external Provider configuration, Secret references, endpoint policy, configurable budgets, and Rig-backed model assembly while preserving the no-Secret demo path.
