---
loreloom-agent: "patch:fix"
loreloom: "patch:fix"
---

Show HTTP status names and distinct recovery guidance for authentication, permissions, rate limits, and server failures. Preserve typed network failure causes and OS error codes, and explicitly identify failures without an HTTP response.

Verify DeepSeek adapter errors through local HTTP responses and retain status details in player notices and TUI rendering without exposing raw provider bodies or credentials.
