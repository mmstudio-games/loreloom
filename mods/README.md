# Loreloom Mods

This directory contains Mod Packages distributed with the Loreloom source tree. Built-in content
and external packages use the same validation, registry, factory, and persistence path.

The package format is intentionally not frozen by workspace scaffolding. Add package data only
after the corresponding content schema gate is resolved.
